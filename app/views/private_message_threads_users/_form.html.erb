<%= form_for(@private_message_threads_user) do |f| %>
  <% if @private_message_threads_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@private_message_threads_user.errors.count, "error") %> prohibited this private_message_threads_user from being saved:</h2>

      <ul>
      <% @private_message_threads_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :private_message_thread_id %><br>
    <%= f.text_field :private_message_thread_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
